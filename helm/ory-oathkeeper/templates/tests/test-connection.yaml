apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ory-oathkeeper.fullname" . }}-test-connection"
  labels:
{{ include "ory-oathkeeper.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "ory-oathkeeper.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
