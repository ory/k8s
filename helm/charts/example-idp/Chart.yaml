apiVersion: v2
# renovate: image=docker.io/oryd/hydra-login-consent-node
appVersion: "25.4.0"
description:
  A Helm chart for deploying the reference implementation for the User Login and
  Consent Flow in Kubernetes
name: example-idp
version: 0.60.0
type: application
