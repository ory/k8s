apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hydra.fullname" . }}-test-connection"
  labels:
{{ include "hydra.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "hydra.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
