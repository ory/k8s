apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "idp-demo.fullname" . }}-test-connection"
  labels:
{{ include "idp-demo.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "idp-demo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
