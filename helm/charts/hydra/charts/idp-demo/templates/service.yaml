apiVersion: v1
kind: Service
metadata:
  name: {{ include "idp-demo.fullname" . }}
  labels:
{{ include "idp-demo.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "idp-demo.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
