ingress:
  enabled: true

deployment:
  extraEnv:
    - name: FOO
      value: BAR

  extraVolumes:
    - name: dummy
      emptyDir: {}

  extraVolumeMounts:
    - name: dummy
      mountPath: /opt/dummy/

basePath: /auth

test:
  busybox:
    repository: busybox
    tag: stable

config:
  csrfCookieName: "some-random-cookie-name"
